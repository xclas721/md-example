# 架構一致性檢查表（以能力為主軸）

> 目的：以「alert/dashboard 能力」為核心做一致性檢查，而不是系統一對一對照。  
> 使用時機：每次重構提案、每次 PR、每次跨系統調整前後。

## 使用說明

- 一次檢查一個能力域（例如：Overview alert、DIMP alert、iframe redirect）。
- 逐項標記 `PASS / FAIL / N/A`，`FAIL` 必須附上修正計畫或例外說明。
- 若為功能差異（業務正常差異），請標記 `N/A` 並附文件連結（建議指向 `06-decisions-and-open-questions.md`）。
- 先確認該能力在哪些系統存在，再做檢查；不存在的系統不列為缺陷。

## 能力基本資訊（先填）

- 能力 ID：
- 能力名稱：
- 類型：`alert` / `dashboard` / `shared-flow`
- 關聯系統：`ACS` / `3DSS` / `Both`
- 關聯檔案：`04-api-inventory.md` 章節或列
- 備註：

---

## A. 分層與模組邊界

| 檢查項 | 狀態 | 適用系統 | 備註 |
|--------|------|----------|------|
| Controller 僅負責輸入驗證/轉呼叫，不堆疊業務邏輯 |  |  |  |
| Service 負責業務邏輯，無 UI/路由耦合 |  |  |  |
| Utils 只放純工具邏輯（無隱性依賴） |  |  |  |
| Aspect 僅處理權限與上下文，無報表業務邏輯 |  |  |  |
| 常數與設定（ApiConst/Properties）集中管理 |  |  |  |

## B. API 契約與命名

| 檢查項 | 狀態 | 適用系統 | 備註 |
|--------|------|----------|------|
| 路徑命名規則一致（系統前綴可不同） |  |  |  |
| Request DTO 命名與欄位語意一致（issuer/requestor 可因領域不同） |  |  |  |
| 回傳型別策略一致（單值/時序/列表） |  |  |  |
| 錯誤回應與例外處理模式一致 |  |  |  |
| 變數 API 與報表 API 的職責分界清楚 |  |  |  |

## C. 時間與查詢上限

| 檢查項 | 狀態 | 適用系統 | 備註 |
|--------|------|----------|------|
| 時間格式皆為 epoch ms（UTC） |  |  |  |
| 區間語意一致（`[from_time, to_time]`） |  |  |  |
| 預設 interval 規則一致（未傳時） |  |  |  |
| 聚合 API 限制輸出點數，不截斷原始交易 |  |  |  |
| 明細 API 有 row cap 且有截斷標記 |  |  |  |
| 特例時間邏輯（如 DDoS 2m）有文件明示 |  |  |  |

## D. 權限與驗證流程

| 檢查項 | 狀態 | 適用系統 | 備註 |
|--------|------|----------|------|
| Alert API 可由 `X-API-Key` 呼叫（無人登入） |  |  |  |
| Dashboard API 使用 `hitrustSsoToken` + RBAC |  |  |  |
| API Key 與 Token 流程不混用 |  |  |  |
| 識別欄位差異（`X-BANK-ID` / `X-REQUESTOR-ID`）已文件化 |  |  |  |
| 權限失敗回應行為一致（訊息/碼） |  |  |  |

## E. 前端整合流程（Vue -> backend -> iframe -> Grafana）

| 檢查項 | 狀態 | 適用系統 | 備註 |
|--------|------|----------|------|
| 路由頁能正確取得 backend 動態 URL |  |  |  |
| backend 回傳 payload 欄位完整（token/org/time/vars） |  |  |  |
| iframe 載入後 Grafana query 參數與預期一致 |  |  |  |
| 時間參數在前後端與 Grafana 三端一致 |  |  |  |
| 失敗情境（token/API key/timeout）處理一致 |  |  |  |

## F. 功能差異管理（重要）

| 檢查項 | 狀態 | 備註 |
|--------|------|------|
| 已確認差異是否為業務需求（不是遺漏） |  |  |
| 差異是否記錄於 `04-api-inventory.md` 差異表 |  |  |
| 差異是否記錄於 `06-decisions-and-open-questions.md` 已決策區 |  |  |
| AI 提示詞是否明確寫「架構一致優先」 |  |  |

## G. 全域能力覆蓋（本輪總檢）

| 項目 | 狀態 | 備註 |
|------|------|------|
| Alert 能力是否全部納入檢查清單 |  |  |
| Dashboard 能力是否全部納入檢查清單 |  |  |
| 每個能力是否有對應文件（inventory / decision / status） |  |  |
| 未涵蓋能力是否已列入待辦 |  |  |

---

## 結論區（每次檢查填寫）

- 檢查範圍：
- 檢查日期：
- 檢查人：
- FAIL 項目數：
- 是否可進入改碼：`是 / 否`
- 後續行動：
